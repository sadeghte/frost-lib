{
  "name": "frost-lib",
  "version": "1.0.1",
  "author": "",
  "license": "ISC",
  "description": "",
  "keywords": [],
  "main": "support/js/index.js",
  "gypfile": true,
  "scripts": {
	"install": "make install && cd support/js && make install",
	"build-addon": "cd support/js && make install",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "node-addon-api": "^8.2.1"
  },
  "devDependencies": {
    "node-gyp": "^10.2.0"
  }
}
